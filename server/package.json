{
  "name": "flipblog-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --no-warnings --watch src/index.js",
    "start": "node --no-warnings src/index.js",
    "test": "node --no-warnings --env-file=.env.test --test-concurrency=1 --test \"tests/*.test.js\"",
    "lint": "node scripts/lint.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "linkedom": "^0.16.11",
    "multer": "^2.0.1",
    "sanitize-html": "^2.14.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
